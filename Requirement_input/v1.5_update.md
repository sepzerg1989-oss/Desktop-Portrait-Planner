我需要对软件进行一次较大的更新，核心目标是参考当前模特库，场地库的样式，增加**服装库**、**道具库**、**妆容库**、这三个资源库，初步有这几个想法
1. 页面入口： 资源库的入口放在表头，为了使画面符合高级的莫兰迪风格画册风格，不能简单的进行堆放，帮我分析下下面这个入口的方案是否可行
【功能与交互要求】
1. 顶部 Header 仅保留三个主入口：【我的策划】、【素材档案 ▾】、【创意广场】。
2. 当鼠标悬浮或点击【素材档案】时，向下优雅地展出一个横向撑满或大面积悬浮的巨型面板。

【DOM 结构与排版布局】
1. 巨型面板采用 `flex` 布局，左侧为 2/3 宽度的文字目录，右侧为 1/3 宽度的“摄影艺术橱窗”大图区域。
2. 左侧目录分为 2 列，严格使用语义化颜色（bg-paper, text-ink）和无边框极简网格：
   - 第一列（人物与造型）：01 模特档案、02 妆面造型、03 服装搭配
   - 第二列（空间与物品）：04 场地堪景、05 道具中心
   每项前带有小号、低透明度的数字序号（如 01, 02），主文字使用优雅的衬线体（font-serif），鼠标悬浮时颜色加深。
3. 右侧大图区域是一个比例修长的容器（如 `w-72` 或 `w-80`），放置一张垂直构图的艺术摄影作品。图片全出血撑满或带微弱白边，加上 `transition-transform duration-700 hover:scale-105` 的轻柔缩放效果。
4. 整个巨型面板背景为 `bg-paper`，严禁任何硬朗的实线边框，仅使用大面积、极低透明度的弥散阴影 `shadow-[0_25px_50px_-12px_rgba(0,0,0,0.05)]` 托起。

【严格 Guardrails】
- 100% 保留原有路由跳转逻辑和 Vue 状态。
- 所有颜色和间距必须调用我之前配置好的语义化类名，严禁硬编码 Hex 色值。

2. 页面设计
页面的设计可以参考当前模特库和场景库的样式，整体风格要保持一致

3. 功能联动
同样的，需要在策划编辑功能中，实现资源库的一键调用和导入功能，和当前模特，场地模块保持一致